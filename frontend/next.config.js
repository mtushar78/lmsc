/** @type {import('next').NextConfig} */
const nextConfig = {
  // Environment variables are automatically exposed via process.env.NEXT_PUBLIC_*
  // No additional configuration needed for NEXT_PUBLIC_* vars in Next.js 9.4+
}

module.exports = nextConfig
